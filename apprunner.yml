version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "build phase"

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
  command: python3 app.py
  network:
    port: 8080